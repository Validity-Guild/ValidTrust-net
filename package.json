{
  "name": "validity-network",
  "version": "1.0.0",
  "description": "Validity Network - Soroban smart contract vault and reward distribution protocol",
  "main": "index.js",
  "scripts": {
    "build:contract": "cd contracts/vault-contract && cargo build --target wasm32-unknown-unknown --release",
    "test:contract": "cd contracts/vault-contract && cargo test",
    "test:ts": "jest",
    "deploy": "ts-node scripts/deploy.ts",
    "init": "ts-node scripts/initialize.ts"
  },
  "keywords": [
    "stellar",
    "soroban",
    "smart-contracts",
    "vault",
    "rust"
  ],
  "author": "Validity Network Contributors",
  "license": "MIT",
  "dependencies": {
    "@stellar/stellar-sdk": "^12.0.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.7",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
